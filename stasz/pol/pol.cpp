#include <bits/stdc++.h>
#define pb push_back
typedef long long ll;
using namespace std;

constexpr int N = 3e5 + 10;

int friends[N],
    enemy[N];


/* pol.cpp */
int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	int n,m;
	cin >> n >> m;
	for (int i=1; i<=n; i++) {
		friends[i] = i;
	}

	int a, b, c;
	for (int i=0; i<m; i++) {
		cin >> c >> a >> b;
		if (c == 1) {

		} else if (c == 2) {

		} else if (c == 3) {

		} else if (c == 4) {

		}
	}

}
	
